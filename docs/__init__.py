# the inclusion of the tests module is not meant to offer best practices for
# documentation in general, but rather to support the `find_packages` example in
# setup.py that excludes installing the "docs" package